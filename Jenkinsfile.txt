pipeline{
    agent any
    stages
        {
          stage('Start_RasaCore_RasaAction')
            {
            steps
                {                
                    sh 'pwd'
                    sh 'ls -l'
                    sh 'BUILD_ID=dontKillMe nohup rasa run --enable-api --cors "*" --debug &'
                    sh 'BUILD_ID=dontKillMe nohup rasa run actions --debug &'
                }
            }   
        }
    }
